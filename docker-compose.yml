services:
  xiangqi:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persist the SQLite database across container restarts
      - xiangqi_data:/data
    environment:
      - DB_PATH=/data/xiangqi.db
    restart: unless-stopped

volumes:
  xiangqi_data:
